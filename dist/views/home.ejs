<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
    <link rel="stylesheet" href="/css/home.css" />
  </head>
  <body>
    <%- include("./partials/navbar.ejs") %>
    <div class="container">
      <% if (locals.user) { %>
      <h1>
        Hello,
        <span class="fullname"
          ><%= locals.user.firstname + " " + locals.user.lastname %></span
        >
      </h1>
      <p class="catch-msg">Explore the latest anonymous posts from our community</p>
      <% } else { %>
      <h1>Hello, <span class="fullname">Anonymous User</span></h1>
      <p class="catch-msg">Log in to explore and create new anonymous post.</p>
      <form action="/auth/login" method="get">
        <button>Login</button>
      </form>
      <% } %>
    </div>
  </body>
  <script>
  </script>
</html>
